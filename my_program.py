import pgzrun

WIDTH = 800
HEIGHT = 600


def draw():
    screen.clear()
    screen.draw.circle((400, 300), 30, 'white')
    
def line(Surface, color, (x1,y1), (x2,y2), width)
    draw.line(screen, (255,0,255), (20,20), (70,80), 2)
pgzrun.go()
